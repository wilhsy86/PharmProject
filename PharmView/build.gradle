apply plugin: 'com.android.application'

android {
    compileSdkVersion 24
    //buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "com.wilhsy.spinner.pharm"
        minSdkVersion 1
        targetSdkVersion 24
        versionCode 5
        versionName '1.1.5'
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}


dependencies {
    compile project(':Spinner')
}